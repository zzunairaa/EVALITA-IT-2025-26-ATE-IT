# Core dependencies
pandas>=1.5.0
numpy>=1.21.0
scikit-learn>=1.0.0
scipy>=1.7.0
tqdm>=4.64.0
matplotlib>=3.5.0
seaborn>=0.12.0

# NLP libraries
spacy>=3.4.0
# After installing spacy, run: python -m spacy download it_core_news_sm

# Embeddings
sentence-transformers>=2.2.0
torch>=1.12.0  # Required by sentence-transformers

# LLM - Open Source (Hugging Face Transformers)
transformers>=4.30.0
accelerate>=0.20.0  # For faster model loading
bitsandbytes>=0.39.0  # Optional: for 8-bit quantization (reduces memory)

# Jupyter
jupyter>=1.0.0
ipykernel>=6.0.0
notebook>=6.4.0

# Optional: For Hugging Face API (if not using local models)
# huggingface_hub>=0.16.0
